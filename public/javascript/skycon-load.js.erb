  var icons = new Skycons (),
    list  = [
                "<%= @current_condition %>"
              ],
              i;

          for(i = list.length; i--; )
            icons.set(list[i], list[i]);

          icons.play();
